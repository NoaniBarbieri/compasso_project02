import styled from "styled-components";

export const ContainerFeedbackUserSearch = styled.div`
    max-width: 516px;
    width: 100%;
    white-space: pre-line;
    /* background-color: blue; */
    display: flex;
    justify-content: center;

    p {
        font-family: 'Roboto';
        font-size: clamp(20px,3.5vw,25px);
        text-align: left;
        color: #fff; 
    }
`